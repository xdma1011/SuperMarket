# سياق مشروع SupermarketSystem — للقراءة أول أي شغل جديد، بلا استثناء

هذا الملف خلاصة كل القرارات المعمارية والقواعد المتراكمة من جلسات بناء طويلة (باك إند، فرونت إند، تطبيق كاشير أوفلاين). القسم الأول أوامر صاحب المشروع نفسه — إلزامية، بلا نقاش. القسم اللي بعده معلومات معمارية تقنية — يشرح "كيف الكود مبني"، مش أوامر، بس لازم تُحترم لتفادي كسر الأنماط الموجودة.

---

## القسم 1 — قواعد صاحب المشروع (إلزامية، بلا استثناء، بلا تفاوض)

هذي مش اقتراحات وليست قابلة "للتحسين" أو "إعادة التفكير فيها" — نُفّذت بعد نقاش مباشر مع صاحب المشروع، وأي مخالفة لازم إذن صريح منه قبلها.

### 1.1 — Connection String معتمد نهائيًا، ممنوع لمسه
```
Server=localhost\MSSQLSERVER_CR7;Database=sprmrkt;Trusted_Connection=True;TrustServerCertificate=True;
```
لا "تصحيح"، لا "تحسين"، لا تغيير حتى لو بدا خطأ ظاهريًا. لو فيه شك، اسأل، لا تعدّل.

### 1.2 — أرقام إصدارات الحزم ثابتة، بلا تحديث تلقائي
كل مشروع (`API`, `Application`, `Infrastructure`, `CashierApp`) عنده أرقام حزم محدَّدة بدقة (`EF Core 10.0.0`, `Swashbuckle.AspNetCore 7.2.0`, إلخ). حزمة جديدة تُضاف بس لميزة جديدة فعلية — أبدًا ما نحدّث رقم حزمة موجودة "لأنها أحدث" أو "أفضل".

### 1.3 — فحص شامل إلزامي بعد أي تعديل، قبل أي تسليم
بلا استثناء، مهما كان التعديل بسيط:
- توازن الأقواس بكل ملف معدَّل (`{`/`}`, `(`/`)`) — لكل ملف لحاله، لا الملف كامل بس
- تطابق endpoints المسجَّلة بـ`Program.cs` مع الموجودة فعليًا بملفات `Endpoints/*.cs`
- تطابق handlers المستخدمة بالـendpoints مع المسجَّلة بـ`DependencyInjection.cs`
- تطابق `DbSet<T>` بين `IApplicationDbContext` والتنفيذ الفعلي بـ`AppDbContext`
- صفر خرق لاتجاه الاعتمادية (`Application` ما يعتمد `Infrastructure` مباشرة)
- لتطبيق الكاشير (WPF): تطابق كل حدث (`Click`, `TextChanged`, إلخ) معرَّف بـXAML مع وجود الميثود المقابلة فعليًا بالـcode-behind، وتطابق سلسلة استدعاءات منشئات النوافذ (كل نافذة تستقبل بالضبط المعاملات اللي الطرف اللي بيفتحها بيرسلها)

### 1.4 — Migration جديدة دايمًا، أبدًا لا حذف قاعدة بيانات
صاحب المشروع عنده بيانات فعلية بقاعدة بيانات حقيقية شغّالة. أي تعديل Schema (عمود جديد، جدول جديد) لازم Migration إضافية جديدة تُضاف فوق الموجود — **ممنوع كليًا اقتراح "احذف القاعدة وأنشئها من جديد"** كحل لأي مشكلة، مهما بدا أسهل.

### 1.5 — بعد كل دفعة تعديل، سلّم الملفات المتغيّرة فقط (لا المشروع كامل، إلا لو طُلب صراحة)، واذكر بوضوح:
- الملفات بالضبط اللي تغيّرت
- أمر Migration الدقيق لو محتاج (اسم واضح يوصف التغيير)
- تنبيه صريح لو التغيير محتاج خطوة يدوية إضافية

### 1.6 — تصميم الضيافة (Complimentary/الاستهلاك الداخلي): "سماح مع مراجعة" حصرًا
تسجيل ضيافة لصنف **ما يُرفض أبدًا** لتجاوز حد كمية — بس يُعلَّم `NeedsReview=true` لو تجاوز الحد اليومي القابل للتعديل من الإعدادات. نفس المبدأ لأي قرار حسّاس مستقبلي مشابه (خصم كبير، بيع برصيد سالب): **لا نوقف العملية أبدًا لمجرد الشك، نسمح ونعلّم للمراجعة.**

### 1.7 — الويب عربي بالكامل، الكود والتعليقات عربي بالمحادثة والتوثيق
لوحة الإدارة (Angular) عربي فقط، بلا استثناء بأي نص ظاهر للمستخدم. التواصل مع صاحب المشروع نفسه بالعامية الأردنية/العربية، بلا رسمية زائدة.

### 1.8 — منتج جديد بالويب لازم يُربط بفرع + سعر يدويًا (قرار تصميم مقصود، لا خلل)
إنشاء منتج بصفحة الكتالوج **ما بيربطه تلقائيًا بأي فرع**. لازم خطوة منفصلة صريحة (قسم "الفروع والأسعار" بنموذج التعديل) — وإلا المنتج **غير مرئي كليًا لتطبيق الكاشير** (مزامنة الكتالوج تبدأ من جدول `ProductBranches` حصرًا). هذا سبب فجوة حقيقية واجهها صاحب المشروع فعليًا (كتالوج فاضي بالكاشير رغم تعريف منتجات) — **أي دليل استخدام أو رسالة توضيحية مستقبلية لازم تشرح هالخطوتين بوضوح**.

---

## القسم 2 — بنية المشروع

```
src/
├── SupermarketSystem.API/            ← Minimal APIs، endpoints
├── SupermarketSystem.Application/    ← منطق الأعمال (Commands/Queries/Handlers)
├── SupermarketSystem.Domain/         ← الكيانات، القواعد الجوهرية
├── SupermarketSystem.Infrastructure/ ← EF Core، تنفيذ الخدمات
├── SupermarketSystem.AdminWeb/       ← Angular (لوحة الإدارة)
├── SupermarketSystem.CashierApp/     ← WPF (تطبيق الكاشير، Offline-first)
└── tools/HashPassword/               ← أداة مساعدة صغيرة
```
اتجاه الاعتمادية: `Domain` ← `Application` ← `Infrastructure`/`API`.

---

## القسم 3 — أنماط معمارية متكرّرة (معلومات، لا أوامر، بس اتبعها لتفادي كسر الاتساق)

### 3.1 — Code + CodeTitle لكل Enum يُرسل بالـJSON
ممنوع `.ToString()` داخل `Select()` مترجم لـSQL (غير موثوق الترجمة). النمط: اجلب البيانات لذاكرة أول (`.ToListAsync()`)، ثم `switch` صريح بـC# للترجمة. الباك إند يرجّع `StatusCode` (رقم) + `StatusTitle` (نص عربي جاهز).

### 3.2 — Idempotency بمفاتيح صريحة
كل عملية حسّاسة (بيع، دفعة، استرجاع) تحمل `ClientRequestId` (GUID) فريد بقاعدة البيانات. إعادة إرسال نفس الطلب يرجّع نفس النتيجة الأصلية (`WasReplay: true`)، لا يسجّل عملية مكرَّرة.

### 3.3 — خصم مخزون ذري
`IStockOperations.TryDecreaseAsync` — جملة SQL خام واحدة (`ExecuteSqlInterpolatedAsync`)، لا "اقرأ ثم اكتب" بالذاكرة. نفس المبدأ لأي عدّاد يحتاج زيادة/نقصان ذري.

### 3.4 — الفلاتر بتتراكم (AND)، لا تتجاوز بعض — فخ وقعنا فيه أكتر من مرة
لو `endpoint` جوّا مجموعة (`MapGroup`) عندها `RequirePermission(X)`، وحطّينا `RequirePermission(Y)` على الـendpoint نفسه، النتيجة تتطلب X **و**Y معًا، لا Y بس. الحل: لو endpoint محتاج صلاحية مختلفة عن مجموعته، أخرجه للمجموعة برّا (`app.MapGet` مباشرة، لا `group.MapGet`).

### 3.5 — طريقة حساب التكلفة: متوسط مرجّح (Weighted Average)، ليس FIFO

### 3.6 — السعر النهائي دايمًا من السيرفر، لا من العميل
`CompleteSaleCommand` ما بتقبل `UnitPrice` من الطلب إطلاقًا — يُحسب من `ProductBranch.SellingPrice` لحظة وصول الطلب. الكاشير (حتى أوفلاين) بيعرض سعر تقديري محلي بس.

### 3.7 — نمط بناء ميزة جديدة (Backend)
`Command`/`Query` بمجلد `Application/<Domain>/<FeatureName>/` → `Handler` بنفس الملف → تسجيل بـ`DependencyInjection.cs` → `endpoint` بملف `API/Endpoints/<Domain>Endpoints.cs` → Migration جديدة لو محتاج Schema.

### 3.8 — نمط بناء ميزة جديدة (Frontend Angular)
`operations.ts` بمجلد `core/api/operations/` → مكوّن `standalone` بـ`features/<name>/` → توجيه بـ`app.routes.ts` (`canActivate: [requirePermissionGuard('X.Y')]`) → رابط بـ`shared/models/nav-item.ts` → Signals + `firstValueFrom` + رسائل خطأ عربية واضحة.

---

## القسم 4 — تطبيق الكاشير (WPF) — تصميم Offline-first

- قاعدة بيانات محلية: SQLite، مسار `%LocalAppData%\SupermarketSystem.CashierApp\local.db`
- رقم نسخة الكتالوج: `Catalog.Version` بجدول `SystemSettings`، زيادة ذرية مع أي تغيير كتالوج
- مزامنة تلقائية: `BackgroundSyncService` (`DispatcherTimer`، كل `SyncIntervalSeconds` من `appsettings.json`، افتراضي 60 ثانية) — تسحب الكتالوج فقط لو رقم النسخة اختلف، وترسل المبيعات المعلَّقة
- البيع: يُحفظ محليًا **قبل** أي محاولة إرسال (`PendingSale`)، محاولة إرسال فورية (Online-first)، فشل → يضل بالطابور
- البحث (بالباركود أو بالاسم): محليًا بالكامل (SQLite)، صفر اتصال إنترنت لحظة البحث نفسه
- الطباعة: ESC/POS خام، USB (Windows Spooler عبر P/Invoke) أو شبكة (TCP:9100) حسب `PrinterConnectionType` بالإعدادات — دعم العربي غير مؤكَّد 100% على كل موديل طابعة فعلي
- نظام تصميم موحَّد بـ`App.xaml` (`Application.Resources`) — أي `Button`/`TextBox`/`PasswordBox` جديد بياخد التنسيق تلقائيًا، بلا تكرار

---

## القسم 5 — الحالة الحالية

✅ **خلص بالكامل:** الويب (إدارة، كتالوج، مبيعات، مشتريات، إرجاعات، ديون موردين، 15 تقرير، مراجعات، ضيافة، Edit شامل، مخزون حالي، مستوى استهلاك، باركود، الجرد الكامل بـ4 مراحل، قسم الفروع والأسعار)، الكاشير (دخول، مزامنة كتالوج+دفعات+طرق دفع، بيع فعلي محلي، بحث بالاسم والباركود، طباعة حرارية، تصميم موحَّد)

✅ **سدّ فجوات أمنية (30/8/2026):** التقاط IP فعلي (X-Forwarded-For أولًا) و`CorrelationId` بكل سطر `AuditLog` (كان `null` ثابت)، تفعيل SQL Server Temporal Table على `AuditLogs` (أي تعديل/حذف مباشر عبر SSMS بينسخ نسخة قديمة تلقائيًا لجدول `AuditLogsHistory`، صف INSERT-only login منفصل لسه خطوة يدوية اختيارية ما نُفِّذت)، خدمة خلفية `PendingReviewEscalationBackgroundService` بتصعّد أي عنصر "بانتظار مراجعة" (إرجاع أو ضيافة NeedsReview) تجاوز `PendingReview.EscalationThresholdDays` (افتراضي 3 أيام) بدون مراجعة فعلية، وتوثيق قيد المتوسط المرجّح كنقطة عمياء لتلاعب صغير متكرر بـ`GetCurrentCapitalValueQuery.cs`.

⏸️ **معروف ناقص/يحتاج تحسين (كلها بانتظار جهاز حقيقي - مش أولوية حاليًا):**
- طباعة العربي: غير مؤكَّدة على طابعة حقيقية (أول تجربة فعلية لازم تتحقق من قيمة `ESC t` الصحيحة)
- طرق الدفع تحتاج اتصال حي أول مرة كل جلسة كاشير (مش مخزَّنة محليًا بالكامل من أول تشغيل)
- دعم منتجات الدفعات (Batch) أوفلاين بتطبيق الكاشير (WPF) نفسه: الباك إند مغطّى بالكامل باختبارات (`BatchTrackedCatalogSyncAndSaleTests.cs`، 15/9) - صفحة مزامنة الكتالوج ترجّع الدفعات المتوفرة بتاريخ صلاحيتها، وCompleteSaleCommand يخصم من الدفعة المحدَّدة بالضبط. تطبيق الكاشير نفسه (WPF) لسه ما جُرِّب فعليًا - Windows-only، بلا بيئة تشغيل هون - يحتاج جهاز حقيقي.
- **ربط جهاز دفع بنكي (فيزا/POS طرفي) بالبيع** - صاحب المشروع سأل هل ممكن يظهر المبلغ تلقائيًا على جهاز الفيزا وقت البيع. الجواب يعتمد كليًا على الجهاز/البنك اللي رح يُختار (بعض أجهزة البنوك التقليدية بالأردن ما بتدعم ربط ECR إطلاقًا، غيرها بيدعم عبر بروتوكول تسلسلي، أجهزة "ذكية" حديثة بعضها عندها API سحابي مباشر) - **لسه ما اختار جهاز (15/9/2026)**. نرجع للموضوع لما يحدَّد الجهاز/البنك.

✅ **قرار (15/9/2026) — استئناف مزامنة كتالوج منقطعة: لن يُبنى.** صاحب المشروع فوّض القرار ("شوف اذا بفيد، وانت شايف انه جيد ضيفه") - تقييمي: لا يستاهل التعقيد. حجم كتالوج محل صغير محدود (صفحات قليلة)، والمزامنة أصلًا خلفية بلا حجب POS، فإعادة كاملة من الصفحة 1 عند انقطاع تكلف ثوانٍ إضافية بس. بناء استئناف حقيقي (تتبّع مؤشر/cursor، معالجة تغيّر الكتالوج أثناء الاستئناف) يضيف تعقيد وحالات حافة حقيقية (حالة جزئية غير متسقة) مقابل فائدة هامشية لهالحجم. **لن يُنفَّذ إلا لو صاحب المشروع رجع وأصرّ بعد قراءة هالتقييم.**

❓ **تناقض مكتشَف (15/9/2026) — بانتظار قرار صاحب المشروع، لم يُلمَس بعد:**
`CreateProductCommand` الفعلي (`SupermarketSystem.Application/Catalog/CreateProduct/CreateProductCommand.cs`
~سطر 190) بيربط المنتج تلقائيًا **بكل الفروع الفعّالة** فورًا لو انعطى
`SuggestedRetailPrice` وقت الإنشاء - هذا **يخالف حرفيًا** نص §1.8 فوق
("إنشاء منتج ما بيربطه تلقائيًا بأي فرع"). اكتُشف أثناء تجهيز بيانات
تجربة لتطبيق الزبائن (Flutter) - منتج جديد بسعر مقترح ظهر مباشرة على
الفرع بلا أي خطوة "الفروع والأسعار" يدوية. لسه ما لُمس الكود ولا
التوثيق. القرار المطلوب: إما نحدّث §1.8 ليوثّق هالحالة الاستثنائية
(سعر مقترح = ربط تلقائي مقصود لتسهيل الإدخال)، أو نلغي هالسلوك من
الكود ليطابق §1.8 حرفيًا كما هو مكتوب.

✅ **خلص (16/9/2026) — عروض الكمية (Bundle Promotion، "N بسعر كذا") مبنية بالكامل:**
كان موثَّق هون كفجوة ("خصومات الكمية/عروض تلقائية غير مدعومة إطلاقًا") -
انبنى فعليًا بطلب صاحب المشروع المباشر. `Promotion`/`PromotionBranch`
(Domain) - منتج واحد، حزمة كمية/سعر، حد أعلى اختياري بالفاتورة، فترة
صلاحية، تفعيل/تعطيل مستقل لكل فرع (نفس فلسفة `ProductBranch`).
`CompleteSaleCommand` يطبّقه تلقائيًا وقت البيع (بالوحدة الأساسية فقط -
راجع تعليق PROMOTION ASSUMPTION بالكود)، مع Snapshot كامل
(`PromotionId`/`PromotionAmount`/`PromotionTitleSnapshot`) فبيع قديم ما
يتأثر لو العرض انتهى/اتعدَّل لاحقًا. إدارة كاملة بلوحة الإدارة (قسم
"العروض" بصفحة الكتالوج). 318/318 اختبار Backend، 610/610 اختبار
Frontend وقت الإنجاز.

✅ **خلص (17/9/2026) — حساب الربح الحقيقي الشهري (Expense + CapitalTransaction + GetMonthlyProfitStatement):**
طلب صاحب المشروع المباشر: "بدقة شديدة"، لكل فرع لحاله، مع مصاريف دورية
(إيجار/كهرباء/ماء/راتب، معتبَرة نفس فئة بعضها بفترة استحقاق منفصلة عن
تاريخ الدفع الفعلي)، وكشف شهري مستقل بلا أي أثر تلقائي على رأس المال.
- `Expense`/`CapitalTransaction` (`Domain/Finance/`) - سجلات تاريخية بحتة
  (بلا تعديل/حذف، نفس فلسفة `CashDrawerLog`)، `Finance.Manage` صلاحية
  جديدة Master Admin حصرًا (غير موجودة بـ`CashierDefaults` ولا
  `AssistantAdminDefaults` عمدًا).
- `SaleInvoiceItem.UnitCostSnapshot` (nullable) - تكلفة الوحدة **وقت
  البيع بالضبط**: منتج متتبَّع دفعات (Batch) ياخذ `ProductBatch.UnitCost`
  كما هو؛ غير متتبَّع ياخذ متوسط مرجّح من فواتير الشراء المستلمة **حتى
  لحظة هالبيع فقط** (لا كل التاريخ، ولا "الآن" وقت تشغيل تقرير لاحق) -
  فشراء لاحق أبدًا ما يغيّر تكلفة بيع قديم رجعيًا. منتج بلا أي تاريخ شراء
  سابق يسجّل **`null` صراحة**، لا صفر (كان رح يضخّم الربح وهميًا) -
  `GetMonthlyProfitStatement` يستثني هالأسطر من التكلفة صراحة ويعرض
  عددها (`ItemsExcludedNoCostHistory`) كتنبيه.
- **إصلاح جانبي مكتشَف أثناء البناء:** `ReceiveStockTransferCommand` كان
  دايمًا يصفّر تكلفة الدفعة الجديدة بالفرع الوجهة (`unitCost: 0m` ثابت)
  بغض النظر عن تكلفة دفعة المصدر الفعلية - كان رح يضخّم ربح أي بيع لاحق
  من دفعة منقولة بين فروع. انصلح: التكلفة تترحّل من دفعة المصدر
  (`StockTransferItem.SourceProductBatchId`) فعليًا.
- `GetMonthlyProfitStatementQuery`: NetRevenue (بنفس تعريف
  `GetSalesSummaryQuery` تمامًا) ناقص CostOfGoodsSold (تحتسب الكمية
  المرتجعة فعليًا `QuantityReturned` من كل سطر) = GrossProfit، ناقص
  مصاريف نفس (فرع، سنة، شهر) = NetProfit. رأس المال (`CapitalTransaction`)
  **منفصل كليًا** - سحب/إضافة يدوي صريح بس، بلا أي ربط تلقائي بالربح.
- لوحة إدارة جديدة (`/finance`، صلاحية `Finance.Manage`): كشف الربح
  الشهري + تسجيل/عرض المصاريف + تسجيل/عرض حركات رأس المال.
- 326/326 اختبار Backend، 610/610 اختبار Frontend وقت الإنجاز.
- **معروف غير مبني بعد ضمن هالميزة نفسها (لم يُطلَب صراحة):** لا واجهة
  لتعديل/حذف مصروف أو حركة رأس مال مسجَّلة بالغلط (سجل تاريخي بحت بتصميم
  مقصود - تصحيح خطأ بمصروف يحتاج مصروف تصحيحي جديد، لا تعديل القديم).

✅ **خلص (21/9/2026) — فروقات الجرد (نقص/زيادة) صارت تنعكس على كشف الربح الشهري:**
كانت فجوة مكتشَفة (`GetCurrentCapitalValueQuery` بيتأثر فورًا من
`StockMovement` بنوع `StocktakeCorrectionIncrease`/`StocktakeCorrectionDecrease`،
بس `GetMonthlyProfitStatementQuery` ما كان يقرأها إطلاقًا) - صاحب
المشروع قال "ضيفه ك ملاحظة فقط حاليًا" وقت التوثيق، ثم صراحة "1 و7
اشتغلهم" لاحقًا (21/9/2026) فانبنى فعليًا:
- **نقص الجرد** خسارة تنزل من صافي الربح، **فائض الجرد** إيراد يزيده -
  الاثنان محتسبان (سبب الفائض: ممكن يكون فاتورة شراء "مش مدخلة" بعد).
- **الفترة**: شهر اعتماد الجرد نفسه (`StockMovement.OccurredAtUtc`).
- **التقييم**: نفس منطق `UnitCostSnapshot` بالضبط (متوسط مرجّح/تكلفة
  دفعة مباشرة) - حركة بلا تاريخ شراء تُستبعَد صراحة
  (`StocktakeMovementsExcludedNoCostHistory`)، لا تُحتسب بتكلفة صفر.
- **آلية التخزين**: بند محسوب تلقائيًا ("صافي فروقات الجرد") جوّا
  `GetMonthlyProfitStatementQuery` نفسه - **بلا** صف `Expense` فعلي
  (`Expense` مصمَّم لمبلغ موجب وإدخال يدوي بس، فائض الجرد إيراد لا
  مصروف). **فرق جوهري موثَّق بالكود**: هذا الرقم محسوب لحظيًا بكل
  استدعاء للتقرير، لا Snapshot مجمَّد مثل `UnitCostSnapshot` - فاتورة
  شراء رجعية (Backdated) لنفس الشهر ممكن تغيّره لاحقًا.
- `NetProfit = GrossProfit - TotalExpenses + StocktakeSurplusValue - StocktakeShortageValue`.
- لوحة `/finance` تعرض البندين + تنبيه لو فيه حركات مستبعَدة.
- 2 اختبار تكامل جديد (نقص+فائض معًا، واستبعاد بلا تاريخ شراء).

✅ **اختبار تكامل جديد (21/9/2026) — دورة كاملة شراء→بيع→ربح→تقفيل صندوق:**
بطلب صاحب المشروع صراحة ("هل بتقدر تضيف Unit Test لعملية اضافة صنف مع
المورد... وبيعه ويرى هل يعكس على المبيعات والارباح؟ مع تقفيل الصندوق").
`CompleteCashClosingTests.دورة_كاملة_شراء_من_مورد_ثم_بيع_تنعكس_على_المبيعات_والربح_وتقفيل_الصندوق` -
يستخدم الـHandlers الفعلية بالكامل (لا بذر مباشر): `CompletePurchaseInvoiceHandler`
→ `CompleteSaleHandler` → يتحقق من `GetSalesSummaryHandler` و
`GetMonthlyProfitStatementHandler` → `CompleteCashClosingHandler`. يمسك
مشاكل التكامل بين 4 وحدات ما كانت مختبرة سوا بنفس السيناريو من قبل.

💡 **أفكار مقترحة (21/9/2026) — للنقاش فقط، لم تُطلَب، صفر كود:**
عرَضتها كإجابة على سؤال "شو بتفيدنا من أنظمة تانية" (بعيدًا عن
الفوترة/الضريبة بالأردن). كلها متحقَّقة بالكود كفجوات حقيقية موجودة،
مش تخمين:
1. **تنبيه قرب انتهاء الصلاحية** - `ProductBatch.ExpiryDate` مسجَّل
   أصلًا، بس صفر تقرير/تنبيه يستخدمه.
2. **بيع بالدين لزبون دائم ("الدفتر")** - `CompleteSaleCommand` حاليًا
   يرفض أي فاتورة الدفعات فيها أقل من المجموع (تسوية كاملة إلزامية
   لحظة البيع) - دين الموردين متابَع بالكامل بالمشتريات، بلا مقابل
   بجهة المبيعات.
3. ✅ **خلص (21/9/2026) — تقرير هامش الربح لكل منتج.** `GetProductMarginReportHandler`
   (تبويب "هامش الربح لكل منتج" بصفحة التقارير) - إيراد صافٍ لكل منتج
   بفترة معيّنة (`LineTotal - QuantityReturned × UnitPriceSnapshot`، نفس
   صيغة الاسترجاع الفعلية بـ`ProcessReturnCommand` بالضبط لا تقريب) ناقص
   تكلفة (`UnitCostSnapshot`) = هامش + نسبة هامش، مرتّب تنازليًا حسب
   الإيراد. سطر بلا تكلفة معروفة يُستبعَد صراحة ويُعلَّم بعدد لكل منتج.
   3 اختبار تكامل جديد.
4. **متابعة فروقات تقفيل الصندوق عبر الوقت لكل كاشير** - `CashClosing`
   موجود يوميًا، بلا تقرير اتجاه زمني يلاحظ نمط عجز متكرر لكاشير معيّن.
5. **سبب واضح للتلف/الهلاك منفصل عن الضيافة** - حاليًا يندمج مع
   "ضيافة/استهلاك داخلي" أو تعديل مخزون عام بلا تصنيف مالي مستقل.

💡 **أفكار مقترحة - دفعة ثانية (21/9/2026) — للنقاش فقط، لم تُطلَب، صفر كود:**
1. **تسعير جملة/مفرّق** - `ProductBranch` سعر واحد بس لكل منتج بكل فرع،
   بلا سعر منفصل للبيع بكميات جملة لمحلات/زبائن تانيين.
2. ✅ **خلص (21/9/2026) — تقفيل صندوق بالورديات (كانت أولوية خاصة من صاحب المشروع).**
   `CashClosing.ShiftNumber` (افتراضي 1) - القيد الفريد صار
   `(BranchId, BusinessDate, ShiftNumber)` لا `(BranchId, BusinessDate)`
   وحدها (Migration `AddCashClosingShiftNumber`، الصفوف القديمة تاخذ
   ShiftNumber=1 تلقائيًا - إضافي بالكامل، بلا فقدان بيانات). حساب
   "المتوقع" (`ExpectedCash`) ما احتاج أي تعديل - كان أصلًا زمنيًا
   (منذ آخر تقفيل فعليًا بـ`ClosedAtUtc`)، بلا علاقة برقم الوردية. لوحة
   الإدارة عندها حقل "رقم الوردية" بنموذج التقفيل + عمود بالقائمة.
   4 اختبار تكامل جديد + 3 Frontend.
3. **كوبونات/قسائم خصم بكود** - `TargetSurface: تطبيق الزبائن (Flutter)
   حصرًا` (تحديد صريح من صاحب المشروع) - الزبون يدخل/يمسح كود بتطبيقه
   وقت الطلب، لا الكاشير يدويًا. يحتاج تصميم منفصل عن `Promotion`
   الحالي (مرتبط بمنتج معيّن) - كود عام مستقل عن منتج، استخدام واحد لكل
   كود (أو لكل زبون)، وربطه بمسار الطلبات (`Order`/`OrderItem`) الموجود
   أصلًا لتطبيق الزبائن.
4. **طباعة ملصقات أسعار للرفوف** - **بلا تجربة فعلية ممكنة حاليًا**
   (صاحب المشروع صراحة: ما عنده طابعة أصلًا، ولا بقالة فعلية بعد - قبل
   الافتتاح). يُبنى بس افتراضيًا (تنسيق ملصق + باركود)، التحقق الفعلي
   من التوافق مع طابعة حقيقية يصير لاحقًا بعد الافتتاح.
5. **حد ائتماني للزبون** - امتداد طبيعي لفكرة "البيع بالدين" (الدفعة
   الأولى، بند 2) - لو انبنى البيع بالدين، حد أقصى لدين كل زبون يمنع
   تراكم دين غير محدود لزبون واحد.
6. **تنبيه استحقاق دفعة مورد** - تحقّقت: صفر مفهوم "تاريخ استحقاق دفع"
   أو "مدة سماح" لأي فاتورة شراء بالنظام كليًا (`grep`: صفر
   `DueDate`/`PaymentTerms`/`CreditLimit` بكل الـDomain) - دين المورد
   متابَع (كم باقي غير مدفوع)، بس بلا "متى لازم يُدفع".
7. **تكامل واتساب بزنس** - تحقّقت: صفر أي دمج واتساب بالكود (Telegram
   بس موجود حاليًا للزبائن - `TelegramChatLink`). واتساب أشيع استخدامًا
   بالأردن للتواصل التجاري من تيليجرام.
8. **تذكير الزبون بإعادة الشراء** - بناءً على تكرار شراء منتج معيّن
   (مثلًا كل أسبوعين)، تذكير عبر تطبيق الزبائن/تيليجرام "خلص وقت
   الطلبية الجاية" - يحتاج بيانات تاريخية كافية بعد الافتتاح الفعلي.

📝 **ملاحظة مؤجَّلة (13/9/2026، حُدِّثت 17/9/2026) — وحدة حسابات شركاء/توزيع أرباح، لم تُطرَق بعد:**
النظام حاليًا صفر مفهوم "شريك"/"نسبة ملكية"/"رأس مال شريك" - نظام نقطة بيع
ومخزون بس. **حساب الربح الحقيقي نفسه انبنى (راجع فوق مباشرة)** - الفجوة
المتبقية هون محصورة بتوزيع هالربح على شركاء متعدّدين بنسب متغيّرة، لا
حساب الربح بذاته.

**قرارات صاحب المشروع (15/9/2026):**
- نسبة الملكية بين شركاء رأس المال **متغيّرة** حسب رأس المال المضخوخ فعليًا
  (لا ثابتة) - وتنعكس على كل حساب مرتبط بالملكية.
- دقة تتبّع الملكية بالنظام **ليست أولوية عالية** - "الملكية تكون معروفة
  حتى بدون سستم" (يعني حتى لو النظام ما تتبّعها بدقة رياضية كاملة، مقبول
  مبدئيًا - الأولوية لحساب **نسبة الربح** بدقة، لا نسبة الملكية بالضرورة).
- **الشريك المضارب** خارج نسب الملكية/رأس المال كليًا، بس له نسبة ربح
  شهرية متفَق عليها مسبقًا بغض النظر عن رأس المال - يُحسب بالتوازي مع
  بقية الشركاء أصحاب رأس المال.

**قرارات صاحب المشروع الإضافية (20-21/9/2026) — حسمت كل الأسئلة المفتوحة:**

1. **الشراكة على مستوى الفرع لحاله، مؤكَّد.** كل فرع جدول شركاء ونسب
   مستقل عن باقي الفروع (مثال المشروع بالذات: فرع 1 هو وأخته بس، فرع 2
   فيه شريك مضارب إضافي) - حساب الربح والتوزيع يصير لكل فرع لحاله، لا
   رقم شركة واحد مجمَّع.

2. **آلية التوزيع الشهري بالتفصيل الكامل** (مثال حرفي من صاحب المشروع:
   محمد شريك، نصيبه الشهري 500، بيسحب 100 → يصير "100 من أصل 500"،
   المتبقي 400):
   - **"تلقائي" يعني**: ببداية كل شهر بينزل **كشف جاهز ومسجَّل** برصيد كل
     شريك (رقم واضح موثَّق، ممكن الرجوع له بأي وقت) - **مو تحويل فعلي
     تلقائي**. السحب الفعلي نفسه قرار/فعل يدوي منفصل كليًا عن نزول الكشف.
   - **سحب مسبق (سلفة على حساب الشهر الجاي)**: مسموح عادي. لو الشريك
     سحب أكتر من رصيده الحالي، الفرق **ينخصم من رصيد الشهر اللي بعده**
     (يعني الرصيد ممكن يصير سالب مؤقتًا، وينعكس تلقائيًا بكشف الشهر التالي).
   - **الرصيد يتراكم**، مؤكَّد - لو الشريك ما سحب كل نصيبه بالشهر، الباقي
     يترحّل ويتراكم فوق نصيب الشهر الجاي (رصيد مستحق تراكمي، لا يصفّر
     كل شهر).
   - **سلفة الكاشير/العامل العادي**: مؤكَّد **مفهوم منفصل كليًا** عن حصص
     الشركاء - التزام على المحل (Payroll/Advances)، بلا علاقة بربح أي
     شريك.
   - **أثر السحب على الصندوق الفعلي** - **قرار بنمطين، كلاهما مطلوب**:
     1. **افتراضيًا**: السحب ينزل من كاش الصندوق فعليًا (زي `PayOut`
        عادي) - يخصم من الكاش المتوقع بتقفيل الصندوق (`CashClosing`).
     2. **خيار ثاني صريح وقت تسجيل السحب**: "عطيته من جيبي الشخصي" -
        يسجَّل كسحب أرباح بلا أي أثر على كاش الصندوق أو تقفيله (صاحب
        المحل دفع من ماله الخاص، لا من الدرج). لازم فلاغ/نوع واضح لكل
        حركة سحب يميّز المصدر (صندوق مقابل جيب شخصي).
   - **استرجاع صاحب المحل لمبلغ دفعه من جيبه — القرار النهائي (21/9/2026):
     الخيار الأدق، لا الأبسط.** لما يسجَّل سحب "من جيبي الشخصي"، السستم
     **لازم** ينشئ/يزيد تلقائيًا رصيد منفصل واضح — "مستحق لصاحب المحل"
     (Owner Receivable) — بقيمة المبلغ بالضبط، **مستقل كليًا عن أرصدة
     الشركاء وعن `CapitalTransaction` العادية**. السبب الصريح من صاحب
     المشروع: "ممكن يتراكم الربح وانسى شو ظل الو" - يعني **ممنوع** الاعتماد
     على تذكّره يدويًا، ولا الاعتماد على سحب رأس مال عادي غير مربوط
     بالحادثة الأصلية (كان الاقتراح الأول الأبسط، **رُفض صراحة**). لما
     يسترجعها لاحقًا (فعل يدوي صريح، "استرجاع" - نفس فلسفة "سماح مع
     مراجعة" بكل مكان تاني بالنظام: لا استرجاع تلقائي بلا تأكيد بشري)،
     الرصيد المستحق يرجع ينقص/يصفّر. يحتاج تصميمه Domain/Application خاص
     (كيان جديد، مو مجرد إعادة استخدام `CapitalTransaction`) - **لسه ما
     انبنى، بانتظار "ابدأ شغل".**
   - **تسجيل السحب من الكاشير نفسه** (مو بس لوحة الإدارة) - **مطلوب صراحة**:
     زر مخصَّص بشاشة البيع، الشريك (محمد مثلًا) يكبسه بنفسه **حتى لو
     الكاشير (الموظف العادي) داخل بحسابه هو وقتها** - يعني بلا تسجيل
     خروج/دخول، بس تحقق هوية إضافي لحظي قبل السماح بالسحب. طرق التحقق
     المقترحة من صاحب المشروع (لسه ما اختار وحدة نهائية): يوزر/باسورد
     خاص بالشريك، أو Telegram OTP، أو مسح باركود شخصي. **يعمل بردو من
     لوحة الإدارة** (الأولوية الأولى - أقل عرضة لانشغال الكاشير ببيع).

3. **خصومات الكمية محدَّدة بفروع معيّنة** — محلول أصلًا (Promotions
   مبنية، كل عرض بتفعيل/تعطيل مستقل لكل فرع).

**⚠️ صريح ومؤكَّد من صاحب المشروع: هذا القسم كله ملاحظات وقرارات تصميم
موثَّقة فقط - "خذ الملاحظات، لا تشتغل" (21/9/2026). صفر كود انلمس لهاي
الوحدة، صفر بناء، لحد ما يقول صراحة "ابدأ شغل". لا تبدأ أي تنفيذ اعتمادًا
على هالتوثيق وحده مهما بدا كامل/حاسم.

---

✅ **خلص (22/9/2026) — دفعة "اعملهم هذول" (صاحب المشروع فوّض تنفيذ 9 عناصر
صراحة، الفيزا/جهاز الدفع البنكي استُثنيت بطلبه "خليها مش أولوية"):**

1. **تنبيه قرب انتهاء الصلاحية** - `GetExpiringBatchesQuery` (تقرير جديد
   بصفحة التقارير، "قرب انتهاء الصلاحية") - دفعات (`ProductBatch`) برصيد
   فعلي موجب (`Stock.QuantityOnHand > 0`) وتاريخ انتهاء ضمن
   `Inventory.ExpiryAlertThresholdDays` (إعداد قابل للتعديل، افتراضي 14
   يوم) من اليوم. `DaysRemaining` يظهر حتى لو سالب (دفعة منتهية فعلًا
   ومنسية بالمخزون) عمدًا - بلا فلترة.

2. **متابعة فروقات تقفيل الصندوق لكل كاشير عبر الوقت** -
   `GetCashierVarianceReportQuery` (تقرير جديد، "فروقات تقفيل الصندوق
   لكل كاشير") - `CashClosing.Variance` مجمَّع حسب `UserId` بفترة معيّنة:
   عدد التقفيلات، عدد مرات العجز/الزيادة، إجمالي ومتوسط الفرق. الهدف كشف
   نمط متكرر (كاشير معيّن دايمًا بعجز) لا حادثة معزولة واحدة.

3. **سبب تلف/هلاك منفصل عن الضيافة** - `MovementType.WasteOut` (قيمة
   إضافية جديدة، بلا Migration لنفسه لأنه enum مخزَّن كـint) +
   `StockMovement.WasteReason` (عمود nullable جديد، ذو معنى فقط
   لـWasteOut - Migration `AddWasteTrackingToStockMovement`). سبب تصنيفي
   إلزامي (`WasteReason`: منتهي الصلاحية/مكسور/تلف تخزين/أخرى) منفصل عن
   `Reason` الحر الاختياري. `RecordWasteIssueHandler` نسخة كاملة من
   `RecordComplimentaryIssueHandler` (نفس آلية "سماح مع مراجعة" بعتبة
   يومية مستقلة `Waste.DailyReviewThresholdQuantity`)، **بلا** شرط تفعيل
   مسبق على المنتج (`IsComplimentaryAllowed` خاص بالضيافة حصرًا - أي
   منتج قابل للتلف بطبيعته). صلاحية جديدة `Inventory.WasteIssue` (Master
   Admin حصرًا، نفس نمط `Inventory.ComplimentaryIssue`). صفحة إدارة جديدة
   (`/waste`) + تقرير سجل منفصل ("سجل التلف والهلاك"، صفحة التقارير).
   **فجوة حقيقية اكتُشفت وأُصلحت أثناء البناء**: `GetPendingReviewsQuery`
   كان يصنّف **كل** حركة `ManualAdjustment` غير مُراجَعة كـ"ضيافة" بشكل
   ثابت (كانت ComplimentaryOut المستخدم الوحيد لنفس الـReferenceType وقتها
   - التعليق القديم بالكود حذّر من هالضبط بالضبط) - لو ما انصلح، أي تلف
   متجاوز للحد كان رح يظهر بصفحة المراجعات مصنَّفًا غلط "ضيافة". انصلح:
   التصنيف صار مشروطًا بـ`MovementType` الفعلي (`PendingReviewType.WasteIssue`
   جديد). 12 اختبار تكامل جديد (5 للـتلف، 2 للسجل، 1 للمراجعات، زائد
   اختبارات فروقات الصندوق وقرب الصلاحية).

4. **تنبيه استحقاق دفعة مورد** - `PurchaseInvoice.DueDate` (عمود
   `DateOnly?` جديد، Migration `AddPurchaseInvoiceDueDate` - اختياري
   تمامًا، ما إلو علاقة بـStatus). `CompletePurchaseInvoiceCommand` قبل
   `DueDate` اختياريًا وقت إنشاء الفاتورة (حقل جديد بنموذج فاتورة الشراء
   بلوحة الإدارة). `GetSupplierPaymentDueQuery` (تقرير جديد، "تنبيه
   استحقاق دفعة مورد") - فواتير `Received` بـ`DueDate` محدَّد وعليها دين
   متبقٍّ فعلي (`TotalAmount > TotalPaidAmount`) فقط - فاتورة بلا
   `DueDate` (الحالة الشائعة، دفع فوري بلا مهلة) غايبة عمدًا، مش خطأ.
   `DaysRemaining` سالب = متجاوزة الاستحقاق فعلًا (بلا فلترة، نفس فلسفة
   `GetExpiringBatchesQuery.DaysRemaining`). 3 اختبار تكامل جديد + اختبار
   لحفظ `DueDate` فعليًا بـ`CompletePurchaseInvoiceTests`.

5. **بيع بالدين لزبون دائم ("الدفتر")** - `CompleteSaleCommand.AllowCreditSale`
   (افتراضي `false`) - تسوية ناقصة/صفرية مسموحة **بس** لو الفلاغ صريح
   **و**`CustomerId` محدَّد؛ الدفع الزائد مرفوض دايمًا. `RecordSaleInvoicePaymentCommand`
   (تسديد لاحق، `POST /api/v1/sales/{id}/payments`، نفس إصلاح EF Core
   الموثَّق بـ`RecordPurchaseInvoicePaymentCommand`)، `GetCustomerDebtsQuery`
   (`GET /api/v1/sales/customer-debts`، ديناميكي بلا Ledger). لوحة الإدارة:
   عمود "المتبقي" + زر "تسديد دفعة" بصفحة المبيعات، وتبويب "ديون الزبائن"
   بالتقارير.
   **ناقص: تطبيق الكاشير (WPF) ما فيه زر "بيع بالدين" لسه** - لازم يبعت
   `AllowCreditSale=true`؛ Windows-only، ما بينبني/يتجرب هون. حاليًا البيع
   بالدين نفسه ممكن بس عبر الـAPI مباشرة.

**مراجعة شاملة بعد التنفيذ (23/9/2026، بطلب صاحب المشروع "حلل الوضع وشوف
المشاكل") - 5 أخطاء حقيقية انمسكت وانصلحت، كلها بشغل هالدفعة نفسها:**
- **تراجع (Regression) بمسار الطلبات**: النسخة الأولى من البيع بالدين كانت
  تسمح بالدفع الناقص لأي `CustomerId` تلقائيًا - و`CompleteOrderHandler`
  (طلبات تطبيق الزبائن/التوصيل) دايمًا بيبعت `CustomerId`، يعني غلطة مبلغ
  من السائق كانت رح تصير دين صامت بدل ما تفشل. انصلح بالفلاغ الصريح
  `AllowCreditSale` + اختبار يحمي السلوك القديم.
- **تسريب مصاري بالإرجاع**: `ProcessReturnCommand` كان يسقّف الاسترجاع
  بقيمة الإرجاع بس، لا بالمدفوع فعليًا - زبون ياخد بالدين ويرجّع ياخد
  كاش ما دفعه. انصلح: لفاتورة عليها دين متبقٍّ بس، الاسترجاع ≤ المدفوع
  ناقص استرجاعات سابقة (`Return.RefundExceedsAmountPaid`). الفواتير
  المسدَّدة بالكامل (كل البيع العادي) سلوكها بلا أي تغيير.
- **فاتورة ملغاة تبين دين**: الإلغاء بيعكس الدفعات (`TotalPaidAmount`→0)
  وبيخلي `TotalAmount` - فكل فاتورة ملغاة كانت رح تظهر "دين" بزر تسديد،
  والباك إند كان يقبل دفعة عليها. انصلح بالطرفين (`Payment.InvoiceVoided`).
- **مراجعة التلف ما كانت تنحفظ (بند 3)**: صفحة المراجعات ما فيها
  `case 5` - زر "تمت المراجعة" على عنصر تلف ما كان يستدعي أي endpoint بس
  يشيله من الشاشة، يرجع بعد التحديث ويضل يتصعّد. انصلح.
- 4 اختبارات Frontend كانت فاشلة (mock ناقص `post`). انصلح.
- النتيجة: 362/362 Backend، 636/636 Frontend، `ng build` نظيف.

**❓ قرارات تصميم معلَّقة بانتظارك (ما انبنت، محتاجة رأيك):**
1. **إرجاع على بيع بالدين ما بينقص الدين** - `RegisterReturn` بيحدّث
   `TotalReturnedAmount` بس؛ الدين المحسوب (`TotalAmount - TotalPaidAmount`)
   بيضل كامل حتى لو الزبون رجّع البضاعة. هل الإرجاع لازم ينقص الدين
   تلقائيًا؟ (نفس الفجوة موجودة أصلًا بديون الموردين.)
2. **التلف ما بينعكس على كشف الربح الشهري** - بينقص المخزون ورأس المال،
   بس `GetMonthlyProfitStatement` ما بيحسبه خسارة (نفس الضيافة حاليًا).
   هل بدك التلف يظهر كبند خسارة منفصل بالكشف (زي فروقات الجرد)؟

**❓ خطأ صلاحيات حقيقي مكتشَف بالمرور (22/9/2026) — غير متعلّق بأي طلب
حالي، بانتظار قرارك، لم يُلمَس:** أثناء إضافة صلاحية `Inventory.WasteIssue`
لدور Master Admin، لاحظت أن صلاحية `Finance.Manage` (المفروض Master Admin
حصرًا بنص §5 فوق بالتوثيق نفسه) **مربوطة فعليًا بقاعدة البيانات بدور
"مساعد أدمن" (Assistant Admin)، لا Master Admin** -
`IdentityConfigurations.cs` سطر ~676: الـ`RoleId` المستخدم بسطر ربط
`Finance.Manage` هو `5d0b3578-417e-4706-ab9b-fc9a208b6642` (مساعد أدمن)،
مش `50e6125a-cac0-4d82-a0b8-9f3c6fff59d7` (Master Admin) - التعليق فوق
السطر نفسه يقول "Master Admin حصرًا" بس الكود بيربطها بدور تاني فعليًا.
يعني أي مستخدم بدور "مساعد أدمن" حاليًا **عنده وصول فعلي لصفحة المالية
(`/finance`) وتسجيل مصاريف/سحوبات رأس مال** رغم أنك حددت هالصلاحية
Master Admin حصرًا بشكل صريح ("عمدًا غير موجودة بـCashierDefaults ولا
AssistantAdminDefaults"). **لسه ما لمسته** - يحتاج Migration جديدة
(حذف/تصحيح صف `RolePermissions` هذا) لو أكّدت إنه فعلاً خطأ ومو قرار
لاحق غيّرته وما وثّقناه. إذا عندك مستخدمين فعليين بدور "مساعد أدمن" على
قاعدة البيانات الحقيقية حاليًا، هذا يعني عندهم وصول مالي فعلي الآن.

---

📝 **ملاحظات صاحب المشروع (23-24/9/2026) — ملاحظات فقط، صفر كود، بانتظار "ابدأ شغل":**

1. **الإرجاع لازم ينجرد بسرعة (ضد الإرجاع الوهمي):** السيناريو المقلق: كاشير
   يسجّل إرجاع وهمي ويحط المبلغ بجيبه. الموجود حاليًا: كل إرجاع بينحفظ
   كفاتورة إرجاع + بيطلع بالمراجعات + بيرجّع البضاعة للمخزون (فالوهمي
   بيطلع زيادة بالجرد). **اللي بيهم صاحب المشروع**: التركيز على المادة
   المرتجعة بالجرد - الجرد عنده **جزئي لمواد محددة بفترات قصيرة** + **كامل
   شهريًا**. الفكرة المقترحة: قائمة "مواد مرتجعة بانتظار جرد" تلقائية
   (بتنقترح أول شي بالجرد الجزئي، بتطلع لما المادة تنجرد)، ولو الجرد لقى
   فرق بمادة إلها إرجاع قريب → تنبيه عالي الأولوية مربوط بالإرجاع
   والكاشير. **شرط مسبق لازم يتأكد**: هل الجرد الحالي بيدعم جرد جزئي
   لمواد محددة، أو بس جرد كامل للفرع؟

2. **لوحة تنبيهات موحَّدة:** صفحة لحالها أو جزء من الداشبورد الحالي -
   التنبيهات مقسّمة **عالية / متوسطة / منخفضة الأولوية**، والكبس على
   تنبيه بيودّي للصفحة المعنية **والفلتر جاهز**.

3. **مراجعة مركّزة للكود القديم (اقتراح Opus بعد إيجاد 5 أخطاء بالدفعة
   الأخيرة رغم نجاح كل الاختبارات):** مش إعادة بناء - مراجعة بالترتيب:
   (أ) سلسلة المال كاملة: بيع ← دفع ← إلغاء ← إرجاع ← تقفيل صندوق؛
   (ب) الصلاحيات: مطابقة كل صف ربط صلاحية/دور بالـHasData مع التوثيق؛
   (ج) الجرد والتحويل بين الفروع (أثرها على التكلفة والربح)؛
   (د) تطبيق الكاشير WPF (مراجعة كود بس - ما بيشتغل هون). لو التوكنز
   قليلة: (أ) لحاله أول.

4. **مراجعة UI/UX:** لوحة الإدارة (Angular) - بتنراجع بصريًا فعليًا
   (تشغيل بمتصفح + صور شاشة: RTL، خطوط عربية، وضوح، جداول، موبايل).
   الكاشير (WPF) - مراجعة XAML وتدفق البيع (سرعة، باركود/كيبورد، حجم
   الأزرار، حالة الأوفلاين) **بلا رؤية النتيجة** - صاحب المشروع بيبعت
   صور شاشة من جهاز ويندوز للتحقق. الترتيب المقترح: مراجعة بلا تعديل
   أول (قائمة مشاكل مرتّبة)، الكاشير قبل الإدارة.

---

## القسم 6 — أسلوب التواصل المتوقَّع

- عربي/عامية أردنية بالتواصل، بلا رسمية
- فحص شامل ذاتي قبل أي تسليم (راجع 1.3) — لا تفترض الصحة، تحقق فعليًا
- صراحة تامة عن أي فجوة أو خطأ، بلا تهوين أو مبالغة بالثقة
