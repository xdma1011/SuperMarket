<Window x:Class="SupermarketSystem.CashierApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تسجيل الدخول - نظام الكاشير"
        Width="380" Height="440"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="نظام نقاط البيع" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />

        <TextBlock Grid.Row="2" Text="اسم المستخدم" FontSize="12" />
        <TextBox x:Name="UsernameBox" Grid.Row="3" Height="34" Padding="8,0" VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="4" Text="كلمة السر" FontSize="12" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="5" Height="34" Padding="8,0" VerticalContentAlignment="Center"
                     KeyDown="PasswordBox_KeyDown" />

        <TextBlock x:Name="ErrorText" Grid.Row="6" Foreground="Red" FontSize="12" TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <Button x:Name="LoginButton" Grid.Row="8" Height="38" Content="تسجيل الدخول"
                Click="LoginButton_Click" IsDefault="True" />

        <TextBlock x:Name="StatusText" Grid.Row="10" FontSize="10.5" Foreground="Gray"
                   HorizontalAlignment="Center" Text="" />
    </Grid>
</Window>
