<Window x:Class="SupermarketSystem.CashierApp.Views.AdminPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="دخول"
        Width="320" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="كلمة المرور" FontSize="14" Foreground="#1F1B16" />

        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Height="40"
                     KeyDown="PasswordBox_KeyDown" />

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelButton" Grid.Column="2" Width="80" Height="36" Content="إلغاء"
                    Style="{StaticResource SecondaryButton}" Click="CancelButton_Click" />
            <Button x:Name="OkButton" Grid.Column="4" Width="80" Height="36" Content="دخول"
                    Click="OkButton_Click" />
        </Grid>
    </Grid>
</Window>
