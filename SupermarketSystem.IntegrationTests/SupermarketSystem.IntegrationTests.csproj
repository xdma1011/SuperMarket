<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
    <!-- يشغّل الـAPI فعليًا بالذاكرة (in-process TestServer) لاختبارات
         endpoint حقيقية (HttpClient) بلا استضافة Kestrel منفصلة. -->
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.0" />
    <!-- تصفير حالة قاعدة بيانات الاختبار بين الاختبارات (حذف صفوف
         بترتيب يحترم الـFK) بلا الحاجة لإعادة إنشاء القاعدة أو تشغيل
         migrations من جديد كل مرة. -->
    <PackageReference Include="Respawn" Version="6.2.1" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SupermarketSystem.API\SupermarketSystem.API.csproj" />
  </ItemGroup>

</Project>